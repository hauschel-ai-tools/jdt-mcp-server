package org.fixture.codegen;

/**
 * Baseline: no extends, no implements, no type parameters.
 */
public class PlainTarget {
    private int value;

    public int getValue() {
        return value;
    }
}
